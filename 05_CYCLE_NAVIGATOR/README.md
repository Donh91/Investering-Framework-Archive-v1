# 05_CYCLE_NAVIGATOR - Forecast Accountability Mission Card

**Status:** NAVIGATION_ONLY  
**Authority:** NONE_BY_ITSELF  
**Folder role:** Public Cycle Navigator continuity, forecast/range accountability, communication templates and performance tracking.

## Current production-routing notice

Binding current routing owner:

`../00_ARCHIVE_CONTROL/2026-09-14__autonomous-data-authority-transition-v1__canonical.md`

Cycle Navigator does **not** wait for or require manual DATA PING input. Current CN state must be resolved through the autonomous machine-output chain and the current CN pointer/package after framework interpretation/ratification.

If the public site needs a field that the current CN package does not expose, it may consume another existing current autonomous canonical output only when that output is fresh, eligible and public-safe. Otherwise it must fail closed. The site must never revive a historical DATA PING runtime or create a parallel forecast engine merely to fill a UI slot.

## Entering this folder

Cycle Navigator is a public output surface, not an independent market-state authority.

Use the current control-plane owner/pointers and `LATEST_CYCLE_NAVIGATOR_POINTER.json` plus the exact referenced machine package. The legacy standalone Cycle Navigator repository identifier is historical only and must not be used as the current route.

Historical CN issues may legitimately contain DATA PING-derived lineage for their frozen issuance period. That history does not define today's upstream production path.

## Active publication contract

For all future Cycle Navigator issues, use:

`protocols/2026-09-14__weekly-cycle-navigator-publication-contract-v1-2.md`

### Binding X-publication continuity

Cycle Navigator on X is one continuing public series. When the user asks for `Cycle Navigator til X`, `CN til X`, `CN til publicering`, or equivalent, always resolve the latest **actually published and archived** CN post first and use it as the style/structure/tone/numbering/narrative baseline. Then update that continuing format with the current canonical CN evidence.

`CYCLE_NAVIGATOR_X_READY.md` is current evidence/content input, not publication-style authority when a prior published post exists. Never restart the X format from scratch merely because a new weekly X-ready artifact exists. This rule applies across chats and Kompas/Handlekompas threads.

The contract also preserves the late-section analytical sequence when evidence is available:

1. coming-week price ranges;
2. intraday map;
3. `2-3 WEEKS` - one short direction + action sentence;
4. `4-8 WEEKS` - one short cycle direction + high-level action sentence;
5. final takeaway.

The 2-3 week and 4-8 week compass lines are public navigation context but are not part of the normal weekly precision score unless a separate prospective scoring protocol is defined in advance.

For current short-horizon website fields such as `NEXT DAYS`, prefer an existing autonomous canonical output. If none exists or it is stale/not public-safe, publish `NOT PUBLISHED`/`UNAVAILABLE`; do not synthesize a new signal in the presentation layer.

## What this folder should prove

A useful public forecast product should be:

- pre-registered enough to score honestly;
- clear enough that later interpretation cannot move the goalposts;
- benchmarked against simple alternatives;
- explicit about uncertainty and abstention;
- traceable to the framework evidence available at issuance time;
- calibrated over time rather than optimized for memorable wins.

## High-value mission seeds

### 1. Mechanical baseline audit

Compare weekly range, direction and phase output against trivial/mechanical baselines. Improvement over prior framework versions is not enough.

### 2. Precision metric audit

Find metrics that overstate skill because of overlap, broad ranges, Jaccard-style scoring, hindsight tolerance or non-independent periods.

### 3. Failure taxonomy

Separate bad direction, bad range width, bad timing, stale input, censored evidence and valid abstention. Avoid one generic MISS label when mechanism matters.

### 4. Public-language calibration

Check whether confidence wording matches empirical evidence and sample size.

### 5. Forecast lineage

Verify immutable issue-time inputs, exact horizon maturity, outcome source and no post-hoc rewrite.

## Authority ceiling

Default mode is `READ_ONLY`.

Do not alter public forecasts, historical score rows or frozen issue-time state during audit. Do not turn explanatory labels into new portfolio rules.

Do not use a historical DATA PING receipt as current CN evidence unless a current autonomous routing surface explicitly names it.

## Astra-class challenge

A stronger model should attempt to make the product **harder to game**, not just more sophisticated.

Good outcomes include:

- simpler scoring with better calibration;
- proof that a celebrated metric has weak baseline advantage;
- better separation between mechanical forecast accuracy and narrative explanation;
- a tighter reproducible issuance/outcome chain;
- identification of forecast classes where `NO_EDGE` is superior to forced precision.

See:

```text
../00_ARCHIVE_CONTROL/2026-09-14__autonomous-data-authority-transition-v1__canonical.md
../06_RESEARCH_LAB/README.md
../04_MARKET_LEARNING/README.md
../07_PROMPTS_AND_AGENTS/astra/README.md
```
