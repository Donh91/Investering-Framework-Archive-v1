# Privacy boundary

The site receives only the existing `PUBLIC_COMPASS_PROJECTION_v1`. Internal evidence snapshots, source bindings, thresholds, weights, prompts, provider mappings, repository paths and private values are not rendered.
