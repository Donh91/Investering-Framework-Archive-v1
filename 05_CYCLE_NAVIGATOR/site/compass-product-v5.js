(() => {
'use strict';
const URL='./data/compass.json';
const esc=v=>String(v??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;').replaceAll("'",'&#039;');
const words=v=>String(v||'').replaceAll('_',' ').replace(/\s+/g,' ').trim();
const friendlyAction=v=>{const s=String(v||'').toUpperCase();if(/UNAVAILABLE|NOT_PUBLISHED/.test(s))return'WAIT';if(/DE_RISK|PROTECT|EXIT|SELL/.test(s))return'HOLD';if(/DEPLOY|BUY|TOPUP/.test(s))return'ADD';if(/PREPARE/.test(s))return'PREPARE';if(/WAIT/.test(s))return'WAIT';return'HOLD';};
const friendlyStatus=v=>{const s=String(v||'').toUpperCase();if(/HARD_WAIT/.test(s))return'WAIT';if(/UNAVAILABLE|NO_EDGE/.test(s))return'PENDING';if(/DEPLOY|BUY|TOPUP/.test(s))return'ADD';if(/PREPARE/.test(s))return'PREPARE';if(/WAIT/.test(s))return'WAIT';return'HOLD';};
const stateClass=v=>{const s=friendlyStatus(v);return s==='ADD'?'active':s==='PREPARE'?'watch':s==='WAIT'?'wait':s==='HOLD'?'hold':'unknown';};
function copy(raw){let s=words(raw);if(!s)return'Awaiting enough evidence for a public call.';return s.replace(/HOLD DEFENSIVE WAIT/gi,'hold current positioning').replace(/HARD WAIT/gi,'wait').replace(/ETHBTC/gi,'Ethereum vs Bitcoin').replace(/proxy breadth/gi,'market participation').replace(/breadth/gi,'market participation').replace(/native confirmation/gi,'market confirmation').replace(/native/gi,'market').replace(/W\d+/g,'this week');}
function eta(v){const s=String(v||'').trim();if(!s)return'No reliable estimate yet';if(/no fixed eta/i.test(s))return'No reliable estimate yet';return s.replace(/conditional/gi,'if confirmation develops');}
function ageLabel(iso){const d=iso?new Date(iso):null;if(!d||Number.isNaN(+d))return'Awaiting fresh data';const m=Math.max(0,Math.floor((Date.now()-d)/60000));return m<60?`${m} min ago`:`${Math.floor(m/60)}h ${m%60}m ago`;}
function nextDaily(iso){const d=iso?new Date(iso):null;if(!d||Number.isNaN(+d))return'Awaiting next Compass';const n=new Date(d);n.setUTCDate(n.getUTCDate()+1);const ms=n-Date.now();if(ms<=0)return'Update due';const m=Math.ceil(ms/60000);return m>=60?`${Math.floor(m/60)}h ${m%60}m`:`${m} min`;}
function horizon(c,key,label){const h=c.horizons?.[key]||{};return `<article><span>${esc(label)}</span><strong>${esc(h.label||words(h.expected_direction)||'Pending')}</strong><p>${esc(copy(h.expected_path))}</p><small>ETA · ${esc(eta(h.eta))}</small></article>`;}
function ladder(c){const rows=Array.isArray(c.capitalization_ladder)?c.capitalization_ladder:[];return `<div class="capital-line">${rows.map((r,i)=>`<div class="capital-step state-${stateClass(r.status)}"><div class="capital-node">${i+1}</div><div class="capital-copy"><div class="capital-title"><strong>${esc(words(r.segment).replace('LARGE CAPS','Large caps').replace('MID CAPS','Mid caps').replace('SMALL CAPS','Small caps').replace('MICROCAPS','Microcaps'))}</strong><b>${esc(friendlyStatus(r.status))}</b></div><p>${esc(copy(r.reason))}</p><small>ETA · ${esc(eta(r.eta))}</small></div></div>`).join('')}</div>`;}
function render(c){const root=document.getElementById('productNow');if(!root)return;if(c?.contract!=='PUBLIC_COMPASS_PROJECTION_v1'||c?.data_status!=='OK'){root.innerHTML=`<section class="fail-card"><small>MARKET COMPASS</small><h2>Fresh navigation is temporarily unavailable</h2><p>No short-horizon signal is inferred from prices alone. The weekly Cycle Navigator remains unchanged.</p></section>`;return;}
 const action=friendlyAction(c.action_now),issued=new Date(c.issued_at_utc),market=copy(c.market_now?.summary),h12=c.horizons?.NEXT_12H||{},h13=c.horizons?.NEXT_1_3D||{},h57=c.horizons?.NEXT_5_7D||{};
 root.innerHTML=`<div class="freshness-strip"><div><span>MARKET COMPASS UPDATED</span><strong>${Number.isNaN(+issued)?'Awaiting fresh data':issued.toLocaleString([],{day:'2-digit',month:'short',hour:'2-digit',minute:'2-digit'})}</strong><small>${esc(ageLabel(c.issued_at_utc))}</small></div><div><span>NEXT DAILY COMPASS</span><strong>${esc(nextDaily(c.issued_at_utc))}</strong><small>Daily navigation freeze</small></div><div><span>NEXT MEANINGFUL CHANGE</span><strong>${esc(eta(c.next_meaningful_change_eta))}</strong><small>Current evidence window</small></div></div>
 <section class="action-hero"><div class="action-label">MARKET COMPASS</div><div class="action-word">${esc(action)}</div><p>${esc(market)}</p><div class="phase-line">Market now · ${esc(words(c.market_now?.directional_state||'MIXED'))}</div><div class="action-grid"><article><span>NEXT 12 HOURS</span><p>${esc(copy(h12.expected_path))}</p><small>ETA · ${esc(eta(h12.eta))}</small></article><article><span>NEXT 1–3 DAYS</span><p>${esc(copy(h13.expected_path))}</p><small>ETA · ${esc(eta(h13.eta))}</small></article><article><span>NEXT 5–7 DAYS</span><p>${esc(copy(h57.expected_path))}</p><small>ETA · ${esc(eta(h57.eta))}</small></article></div></section>
 <section class="now-section"><div class="section-title"><div><small>CAPITAL ROTATION · OFFICIAL COMPASS</small><h2>Bitcoin → microcaps</h2></div><p>Where risk belongs now, and what has to happen before capital should move further down the risk curve.</p></div>${ladder(c)}</section>
 <section class="now-section"><div class="section-title"><div><small>TIME HORIZONS</small><h2>What matters next</h2></div><p>The daily Compass is short-horizon navigation. The weekly Cycle Navigator remains the frozen cycle outlook.</p></div><div class="horizon-grid">${horizon(c,'NEXT_12H','NEXT 12 HOURS')}${horizon(c,'NEXT_1_3D','NEXT 1–3 DAYS')}${horizon(c,'NEXT_5_7D','NEXT 5–7 DAYS')}${horizon(c,'CYCLE_ALTCOINS_3_8W','ALTCOIN CYCLE · 3–8 WEEKS')}</div></section>`;
}
async function load(){try{const r=await fetch(`${URL}?v=${Date.now()}`,{cache:'no-store'});if(!r.ok)throw Error(r.status);render(await r.json());}catch{render(null);}}
function boot(){load();setInterval(load,5*60*1000);}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(boot,50));else setTimeout(boot,50);
})();
