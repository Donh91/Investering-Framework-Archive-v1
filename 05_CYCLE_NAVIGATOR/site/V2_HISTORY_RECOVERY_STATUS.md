# Historical recovery handoff

CN21+ are already present in canonical repository namespaces and should be recovered from repository evidence before asking the user for manual copies.

## Provenance correction

The early manual-era public record must distinguish raw archived X post text from score evidence recovered from a later published issue. Do not label a record `user-supplied original` unless the original post text was actually supplied or independently recovered.

- CN #1, #2, #3, #4, #5, #7, #8, #9, #11, #12, #13, #14 and #15 have full archived published-X copies under `canonical-project-archive/06_CYCLE_NAVIGATOR/published-x-posts/`.
- CN #6 and #10 remain raw-post gaps in the legacy published-X register. Their point-in-time score evidence is nevertheless recoverable from the next published issue, CN #7 scores CN #6 and CN #11 scores CN #10. Preserve those published scores as `PUBLISHED_MANUAL_AUDIT`, but do not claim the missing raw post itself has been recovered until it has.
- CN #16 onward must use locked/canonical post, forecast, score and commit-history evidence. Any binding note must identify its actual source and must not upgrade derived evidence into an original publication.

Historical public scores are records of what was published at the time. They must not be retroactively normalized or invalidated because later automated scoring uses stricter tooling. Preserve era and method labels so the public scoreboard remains honest without erasing the manual-era track record.

Any further gap discovery is machine/repository work first. User intervention is warranted only for a specific residual original that remains unavailable after repository/history recovery.