# Authority split

`data/compass.json` is presentation-only public navigation derived from the hash-verified official Compass projection. It has no portfolio execution authority.

CoinGecko remains display context only. It must never alter Market Compass action, direction, ETA or capitalization status.

The weekly CN package must never be rewritten by Compass. Compass may differ at shorter horizons; the public product may show both without reconciliation.
