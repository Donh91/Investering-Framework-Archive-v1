# Implementation scope

Current phase: wire generator into hourly native chain, add tests/CI, add provider/budget visibility, add bounded persistent failure repair/readback, then graduate through PR/main readback.
