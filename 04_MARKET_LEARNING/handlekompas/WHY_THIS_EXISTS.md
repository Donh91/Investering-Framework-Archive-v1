# Why this exists

Recent manual Custom-GPT Data Ping runs demonstrated stale pointer/cache reads and one audit-invalid run with missing call receipts, while GitHub owners remained fresher. Native Handlekompas removes that redundant manual dependency and reads the owner-bound aggregate instead.
