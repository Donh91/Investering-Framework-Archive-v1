# Chat presentation note

Investering-project readbacks are plain text/JSON by default. No widgets or rich UI should be required for Handlekompas/Data Ping/CN/OTA readbacks unless the user explicitly asks for them.
