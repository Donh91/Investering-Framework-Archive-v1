# Historical Shadow Sensor Validation — Executive Verdict

Status: `RESEARCH_ONLY_NON_CANONICAL`

## Bottom line

Stage 2 does **not** validate historical edge for any recovered legacy sensor. Stage 1 recovered 14 sensor definitions/concepts, but reconstructed **0 exact eligible historical sensor-event states**. Therefore historical hit rates, false-positive rates, false-negative rates, lead-time distributions, regime performance, outcome returns and baseline superiority are scientifically unavailable.

The dominant finding is not performance, but **identity quality and research triage**.

- `EARLY_ROTATION_PRE_TRIGGER_V1_1`: exact historical definition, no historical event sample. Worth a separate preregistered forward test, specifically against its own components.
- `FAKE_ROTATION_TYPE3_V2`: exact pattern, no historical event sample. Worth a prospective veto/filter test against a simple `high BTC.D + weak breadth` baseline. The archived `55–75% failure-rate` claim is **NOT REPRODUCED**.
- `CCE_V1_CONFIDENCE_COMPRESSION_ENGINE`: keep as a structural anti-double-counting research control, not as proven alpha. Stage 1 directly establishes nested/shared inputs across several composites.
- `ROTATION_BREADTH_FILTER_V2` and `ROTATION_DOMINANCE_FILTER_V2`: retain as WATCH research components because their historical semantics are comparatively well recovered, but no performance claims are authorized.
- `ROTATION_READINESS_SCORE_V2`: classify REDUNDANT at this stage. It nests Pre-Trigger, breadth and dominance while lacking fully frozen scoring semantics and any eligible historical sample.
- ETF-era classifiers, SRE, FAE, Macro Delay and RWE: historical performance is UNTESTABLE under the frozen evidence. RWE remains blocked from live weighting.

## Scientific consequence

The historical archive does **not** justify promoting or backfilling any legacy sensor into the live framework. Maximum legitimate conclusion is `PROSPECTIVE_TEST_JUSTIFIED`, and only for definitions that can be frozen before new observations.

## Strongest adverse finding

Complexity is more strongly documented than edge. The recovered system contains clear component nesting and common-driver risk, while empirical incremental information is unavailable. The burden of proof therefore sits with future prospective tests, not with historical storytelling.
