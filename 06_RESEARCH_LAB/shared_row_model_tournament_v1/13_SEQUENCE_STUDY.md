# Integrated Sequence Study

Frozen sequences:

1. `ETHBTC persistence -> BTC.D rollover/reclaim -> breadth survival`
2. `BTC ETF absorption -> ETHBTC weakness -> breadth deterioration`
3. `price stress -> liquidation clearance -> OI/funding reset -> reclaim`

Each step is classified on matched rows as one of: `INFORMATION`, `CONFIRMATION`, `REDUNDANT_DELAY`, `VETO`, `TIMING_VALUE`.

Sequence timing is measured against the same outcomes and horizons as candidate comparisons. A later step is not credited merely for describing an already-known state. Confirmation delay must be charged against false-negative opportunity cost.

Status: `FROZEN_PENDING_SHARED_ROWS`.
