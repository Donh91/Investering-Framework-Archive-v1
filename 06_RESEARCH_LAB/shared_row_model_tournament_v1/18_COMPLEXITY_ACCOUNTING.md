# Complexity Accounting

Complexity is reported, not used as a hidden objective.

Per candidate record: number of independent information families, named transforms, providers, revision-prone inputs, missingness dependencies, sequence gates and operational owner contracts.

A larger stack is penalized only in explicit sensitivity analysis. It is never rejected for size alone. If Full Stack supplies reproducible unique decision value that survives robustness controls, it wins.
