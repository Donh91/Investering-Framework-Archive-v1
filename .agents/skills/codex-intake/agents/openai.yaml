interface:
  display_name: Codex Intake
  short_description: Route bounded reproducible research defects into Codex governance.
  default_prompt: Verify that the finding is bounded and reproducible, deduplicate against current remediation authority, preserve the allowed and forbidden scope, and prepare a governed Codex research candidate. Never self-declare CODEX_READY or execute the fix inside this skill.
policy:
  allow_implicit_invocation: false
