interface:
  display_name: Developer Source Research
  short_description: Verify external technical behavior from primary sources.
  default_prompt: Resolve the external technical uncertainty using sanitized primary developer sources. Keep current local repository authority separate, verify load-bearing sources, record fallback and conflicts, and do not authorize code or framework changes.
policy:
  allow_implicit_invocation: true
