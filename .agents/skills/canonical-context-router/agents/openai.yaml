interface:
  display_name: Canonical Context Router
  short_description: Resolve current Investering authority before reasoning.
  default_prompt: Resolve the current repository authority, active owner files, relevant addenda and explicit conflicts for this task. Return a small verified context packet and do not solve the downstream market or framework task inside the skill.
policy:
  allow_implicit_invocation: true
