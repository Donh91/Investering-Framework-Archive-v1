interface:
  display_name: Archive Governance
  short_description: Govern safe, non-duplicative Investering repository writes.
  default_prompt: Classify the durable unit, search for the existing owner, verify explicit write intent and the non-default task branch, preserve destructive-authority separation, apply repository safety and return a governed archive decision with readback validation.
policy:
  allow_implicit_invocation: false
