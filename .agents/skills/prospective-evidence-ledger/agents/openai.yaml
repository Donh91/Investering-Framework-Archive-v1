interface:
  display_name: Prospective Evidence Ledger
  short_description: Govern existing prospective evidence row lifecycles.
  default_prompt: Resolve the registered test and ledger owner, verify causal pre-registration, frozen fields, maturity, source lineage, duplicates, event windows and owner-defined validation. Do not create tests, schemas, scores, promotion authority or portfolio actions.
policy:
  allow_implicit_invocation: false
