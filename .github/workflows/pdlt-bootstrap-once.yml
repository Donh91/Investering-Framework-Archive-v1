# framework-lifecycle: EXPECTED_BLOCK
# framework-lifecycle-reason: PDLT_METHODS_FROZEN_ZERO_NEW_CFGI_CREDITS_AUTHORIZED
# framework-lifecycle-since: 2026-09-02T14:30:46Z
# framework-expected-exit: 78
name: PDLT historical bootstrap once (frozen)

# The methods-hardening contract authorizes zero new CFGI credits while PDLT
# discovery remains frozen. Remove the latent push-triggered 12,300-credit path.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  bootstrap_frozen:
    runs-on: ubuntu-latest
    steps:
      - name: PDLT historical bootstrap frozen pending explicit reopen
        run: |
          echo 'PDLT_METHODS_FROZEN_ZERO_NEW_CFGI_CREDITS_AUTHORIZED'
          exit 78
