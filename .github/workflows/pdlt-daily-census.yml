# framework-lifecycle: EXPECTED_BLOCK
# framework-lifecycle-reason: PDLT_METHODS_FROZEN_NO_ADMISSIBLE_PROSPECTIVE_RUNTIME
# framework-lifecycle-since: 2026-09-02T14:30:46Z
# framework-expected-exit: 78
name: PDLT daily prospective census (frozen)

# PDLT discovery remains methodologically frozen and the required frozen model
# is not present on main. Keep this workflow observable but non-scheduled and
# read-only until the explicit reopen contract is satisfied.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  census_frozen:
    runs-on: ubuntu-latest
    steps:
      - name: PDLT prospective census frozen pending methods reopen
        run: |
          echo 'PDLT_METHODS_FROZEN_NO_ADMISSIBLE_PROSPECTIVE_RUNTIME'
          exit 78
