# framework-lifecycle: EXPECTED_BLOCK
# framework-lifecycle-reason: PDLT_MATURATION_FROZEN_NO_PROSPECTIVE_FORECAST_LEDGER
# framework-lifecycle-since: 2026-09-02T06:28:10Z
# framework-expected-exit: 78
name: PDLT outcome maturation frozen

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  maturation_frozen:
    runs-on: ubuntu-latest
    steps:
      - name: PDLT maturation frozen until prospective forecasts exist
        run: |
          echo 'PDLT_MATURATION_FROZEN_NO_PROSPECTIVE_FORECAST_LEDGER'
          echo 'The PDLT forecast ledger is absent, so scheduled Binance collection and zero-outcome PASS runs have no evidence value.'
          echo 'Reopen only after a validated frozen model has produced prospective, preregistered forecasts.'
          exit 78
