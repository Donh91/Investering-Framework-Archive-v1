# framework-lifecycle: EXPECTED_BLOCK
# framework-lifecycle-reason: PDLT_METHODS_FROZEN_NO_ADMISSIBLE_FORECAST_INPUT
# framework-lifecycle-since: 2026-09-02T14:30:46Z
# framework-expected-exit: 78
name: PDLT outcome maturation (frozen)

# No admissible PDLT forecast input exists on current main. Preserve historical
# code/data, but do not schedule a direct-main maturation writer while discovery
# and prospective census are methodologically frozen.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  maturation_frozen:
    runs-on: ubuntu-latest
    steps:
      - name: PDLT maturation frozen pending prospective reopen
        run: |
          echo 'PDLT_METHODS_FROZEN_NO_ADMISSIBLE_FORECAST_INPUT'
          exit 78
