name: Automation Production Health Gate

on:
  pull_request:
    paths:
      - '.github/workflows/**'
      - 'scripts/health/**'
      - 'scripts/architecture/**'
      - 'scripts/orchestration/build_framework_handoff_manifest.py'
      - 'tests/health/**'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install test dependency
        run: python -m pip install --disable-pip-version-check pytest
      - name: Compile health and handoff code
        run: python -m py_compile scripts/health/build_automation_health.py scripts/health/build_architecture_health.py scripts/health/build_operations_dashboard.py scripts/health/check_writer_trigger_safety.py scripts/architecture/build_latest_handoff.py scripts/orchestration/build_framework_handoff_manifest.py
      - name: Validate main writer configuration
        run: python scripts/health/check_writer_trigger_safety.py --workflow-root .github/workflows --output /tmp/writer-trigger-safety.json
      - name: Run health unit tests
        run: python -m pytest -q tests/health/test_automation_production_health.py tests/health/test_architecture_health_v2_1.py tests/health/test_operations_dashboard.py tests/health/test_writer_trigger_safety_v2.py tests/health/test_observer_and_orchestration_semantics.py
      - name: Run static repository inventory without API
        run: |
          python scripts/health/build_automation_health.py --workflow-root .github/workflows --json-output /tmp/automation-health.json --md-output /tmp/automation-health.md || code=$?
          test "${code:-0}" -ne 2
          python - <<'PY'
          import json
          data=json.load(open('/tmp/automation-health.json'))
          assert data['contract'] in {'AUTOMATION_PRODUCTION_HEALTH_v2','AUTOMATION_PRODUCTION_HEALTH_v2_1'}
          assert data['workflow_count'] >= 1
          assert len(data['workflows']) == data['workflow_count']
          PY
