# API Gateway Luna Smoke Test

Status: CONTROLLED_ONE_TIME_TEST

Purpose: verify OPENAI_API_KEY, GPT-5.6 Luna Responses API access, strict structured output, token accounting, cost receipt and authority boundaries.

The temporary pull-request workflow invocation must not be merged as a permanent trigger. No scheduled API usage, framework-state change, model-weight change, canonical promotion or portfolio action is authorized.
